"use strict";
// create two objects //
const book = {
    name: "ESSENTIAL TYPESCRIPT",
    price: 450
};
const apple = {
    name: "apple",
    price: 200
};
console.log(`book name : ${book.name}, price : $${book.price}`);
console.log(`fruite name  : ${apple.name}, price : $${apple.price}`);
